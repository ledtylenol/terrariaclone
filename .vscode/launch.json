{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Odin",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/bin/game.exe", // Replace with the path to your executable
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Run Odin",
        }
    ]
}